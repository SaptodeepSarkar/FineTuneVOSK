# FineTuneVOSK runtime dependencies (Python side).
# Install with:  pip install -r requirements.txt
#
# NOTE: the actual VOSK (Kaldi) training runs inside Docker or a host Kaldi
# build — see scripts/setup_kaldi.sh. This file covers the Python automation
# (audio normalization + Whisper transcription + live recording).

# ---- Transcription engine (Whisper) --------------------------------------
# Used for BOTH offline label generation (fine-tune pipeline) and live
# microphone transcription. CTranslate2 backend — fast, low-RAM, GPU/CPU.
# VOSK is the model being FINE-TUNED; it is NOT used for transcription.
faster-whisper>=1.0.0

# ---- Live microphone recording (scripts/live_transcribe.py) --------------
sounddevice>=0.4.6
numpy>=1.24

# ---- Testing the fine-tuned model (scripts/test_model.py) ----------------
vosk>=0.3.45